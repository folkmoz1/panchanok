import dbConnect from "../../../../utils/dbConnect";


export default async (req, res) => {
    const {
        method,
        query: { postId },
    } = req

    switch (method) {
        case 'GET':
            try {

            } catch (e) {

            }
            break
    }
}
