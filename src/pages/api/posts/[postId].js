import dbConnect from "../../../../utils/dbConnenct";


export default async (req, res) => {
    const {
        method,
        query: { postId },
    } = req

    switch (method) {
        case 'GET':
            try {

            } catch (e) {

            }
            break
    }
}
